# Security Policy

## 秘密情報を報告へ含めない

脆弱性を報告するときも、Discord Botトークン、OpenAI APIキー、GoogleサービスアカウントJSON、実在するDiscord ID、会議URL、SQLiteデータベースをIssueへ貼らないでください。

秘密情報を誤って公開した場合は、Git履歴から消すだけでは不十分です。該当トークンやキーを直ちに失効し、再発行してください。

## 対応範囲

安全上の問題は、公開Issueへ再現用の架空データだけを載せて報告してください。実データが必要な場合は、リポジトリ所有者が案内する非公開経路を使ってください。

このテンプレートは次を安全境界とします。

- `.env`、`data/`、鍵、ログをGit管理しない
- URL、Discord識別子、呼び名、テンプレート、DM本文をOpenAI入力から分離する
- DM送信時に対象サーバーへの在籍と人間ユーザーであることを再確認する
- 管理操作と本人操作の権限を分離する
- Discordメンションを既定で無効化し、予定時刻に「参加」と回答済みのDiscordユーザーIDだけを明示許可する
- SQLiteの配信claimで再起動や同時tickによる重複送信を防ぐ
- VC音声は全参加者のセッション単位同意後だけ受信し、新規参加者が入ったら全受信を停止する
- VC音声・生文字起こし・分析はローカルでAES-256-GCM暗号化し、開始から固定24時間で削除する
- 音声認識はローカル`faster-whisper`だけを使い、音声をOpenAIや外部STTへ送らない
- AI要約には匿名化済み文字起こしだけを渡し、裏取りには検査済みの短い公開事実だけを渡す
- VC専用チャンネルは`@everyone`の閲覧明示拒否、固定guild/channel、メンション無効化を必須にする
