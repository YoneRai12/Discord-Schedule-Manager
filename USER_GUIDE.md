# Discord Schedule Manager かんたん使い方

このBotは、会議の「いつ・何の会議・URL・誰が来る・いつ知らせる」をDiscordだけでまとめます。難しい操作はありません。

## まず覚える3つ

1. Botへ話すときは、通常チャンネルで必ず`@予定管理`を付けます。
2. Botが黄色い確認画面を出したら、内容を見てから「登録する」を押します。
3. 招待された人は、ボタンでもDMの普通の文章でも回答できます。
4. DM送信が一時的に失敗したりBotが再起動したりしても、未送信分は自動で再試行します。

## 管理者が最初にすること

### 1. メンバーの呼び名を覚えさせる

Discordで対象の人を選び、Botへ次のように伝えます。

```text
@予定管理 @対象の人 を メンバーA として登録して
```

Botは「メンバーA」が誰かを、このサーバー内だけで記憶します。同じ操作は`/meeting member-add`でもできます。

約15人を最初に登録すれば、会議ごとにDiscordアカウントを選び直す必要はありません。

### 2. いつもの参加者をテンプレートにする

```text
@予定管理 テンプレート「全体定例」として
参加者: メンバーA、メンバーB、メンバーC
を保存
```

続けて、毎回使うテンプレートにします。

```text
@予定管理 テンプレート「全体定例」を既定にして
```

これで、会議を作るときに参加者を書かなければ「全体定例」の人へ自動でDMします。

テンプレートには好きな名前を付けられます。たとえば「全体定例」「運営だけ」「レビュー担当」のように分けられます。

## 会議を作る

Botへ会議名・日時・URLをまとめて伝えます。順番はバラバラで構いません。固定メンバーの登録済みの呼び名やDiscordメンションも途中へ混ぜられます。

```text
@予定管理 メンバーA メンバーB 30分前 全体定例
https://calendar.app.google/招待用トークン 来週月曜20時30分 1時間
```

Googleカレンダーの招待URLでも大丈夫です。Botが招待ページからGoogle Meetのリンクを自動で探します。Google側の表示によって見つけられない場合だけ、直接のGoogle Meet URLを貼ってください。Meet・Zoom・Teams・Whereby・JitsiなどのHTTPS会議URLも使えます。招待URLや見つけた会議URLはAIへ送りません。

日時やURLが足りなければ、Botが足りない項目名を返します。会議名だけ無い場合は日時から仮の名前を付け、確認画面に「自動設定」と表示します。

Botが黄色い確認画面を出します。次の内容を確認してください。

- 会議名
- 日付と時刻
- 終了予定
- URLが登録済みになっているか
- 誰へ個別DMを送るか

正しければ「登録する」、間違っていれば「取り消す」を押します。「登録する」を押すまでは、会議もDMも確定しません。

### URLだけあとから直す

`@予定管理`を付けてURLを送ります。通常チャンネルでは、会議カードへの返信でもBotメンションが必要です。

```text
@予定管理 https://calendar.app.google/招待用トークン
```

会議がそのチャンネルに1件だけなら、`@予定管理 URL`だけで更新候補を出します。`@予定管理 全体MTGのGoogleミートをこのURLに変えて`のような普通の文章も使えます。複数の会議があって対象を決められない場合は、Botが候補を表示し、勝手には更新しません。

### 別の参加者テンプレートを使う

会議の文章に次を足します。

```text
参加者テンプレート: レビュー担当
```

### 今回だけ個別DMを送らない

```text
参加者: なし
```

### AIを使わずに作る

`/meeting create`を使い、表示された項目へ直接入力します。

## 招待された人が答える

### ボタンで答える

会議カードまたはDMの「参加」「未定」「欠席」を押します。あとから押し直しても大丈夫です。
開始通知では、その時点で「参加」になっている人だけがメンションされます。「未定」「欠席」「未回答」の人はメンションされず、参加者が0人ならメンションなしで通知だけ届きます。

### DMへ文章で答える

```text
参加します
```

```text
まだ分からないので未定です
```

```text
今回は欠席します
```

Botは「確認しました」と返信し、会議名・日時・現在の回答・自分の通知時刻を見せます。DMの確認には会議URLボタンも付きます。

会議が複数あるときは、DMに書かれている会議IDも付けます。新しい会議は8文字で、以前作られた7文字IDも使えます。

```text
MEET0001 参加します
```

## 自分だけの通知時刻を決める

招待DMへ次のように返信できます。

```text
1時間前と10分前に通知して
```

この書き方は、対象が1つに決まる場合に「今回の会議」と「今後の既定」の両方へ保存します。

今回だけ変える場合:

```text
今回は30分前と5分前に通知して
```

今後の会議だけ変える場合:

```text
今後は毎回1時間前と10分前に通知して
```

通知を止める場合:

```text
今回は通知なし
```

```text
今後は通知なし
```

自分の設定を見るには`/meeting my-reminders`、または通常チャンネルで次のように話します。

```text
@予定管理 自分の通知設定を見せて
```

欠席にした会議の個人通知は送られません。あとから参加または未定へ戻すと、まだ時刻が来ていない通知を作り直します。

## DMではなく通常チャンネルで答える

通常チャンネルでも、Botを直接メンションすれば本人の回答を変更できます。

```text
@予定管理 MEET0001の会議に参加します
```

```text
@予定管理 MEET0001は今回は10分前に通知して
```

チャンネルへ返す確認文には、会議URLをもう一度表示しません。

## 誰が来るかを見る

```text
@予定管理 MEET0001の出欠状況を見せて
```

または`/meeting status`を使います。次の内容が分かります。

- 参加
- 未定
- 欠席
- DMを送ったが未回答
- DMが届かなかった人

会議一覧は`@予定管理 今後の会議を見せて`または`/meeting list`です。

## 管理者がよく使う言い方

```text
@予定管理 テンプレート一覧を見せて
```

```text
@予定管理 テンプレート「全体定例」を見せて
```

```text
@予定管理 MEET0001の招待DMを参加者: メンバーDへ送って
```

```text
@予定管理 全体定例のリンクはこれ https://meet.google.com/new-room
```

```text
@予定管理 MEET0001を中止して
```

これらはすべて`/meeting`コマンドでも実行できます。

テンプレートの内容や呼び名一覧にはメンバー名が含まれるため、通常チャンネルで頼んでも詳細は管理者本人のDMへ届きます。チャンネルには「DMに送りました」とだけ表示されます。

## 安全のための決まり

- 会議を作る・変える・中止する、メンバーやテンプレートを管理する、まとめてDMする操作は管理者だけです。
- 出欠と自分の通知は本人だけが変更できます。
- DMから操作する人が現在もサーバーにいるか、送信前にBotが確認します。
- DM本文そのものは保存しません。決まった結果だけを保存します。
- 会議URL、Discord ID、会議ID、呼び名、テンプレート名、DM本文はOpenAIへ送りません。
- Googleカレンダー招待URLの解析はBotのPCとGoogleの間だけで行い、招待ページ本文は保存しません。自動取得できない招待は直接Meet URLを使います。
- 通常チャンネルではBotを直接メンションしていない会話を自然言語操作として読みません。

OpenAI APIの入出力は、組織がデータ共有へ明示的に同意しない限り、既定ではモデル学習に使われません。ただし、共有トラフィックや無料トークン特典を有効にしたAPIプロジェクトでは、共有した入出力が評価や学習に使われる場合があります。API経路の`store:false`だけでは、この共有設定はOFFになりません。ChatGPTサブスクリプション認証を使うCodex App Serverには`store:false`というAPI指定はなく、APIプロジェクトとは別にChatGPT側のデータコントロールと一時thread削除を使います。どちらの場合も、このBotは設定だけに頼らず、送らなくてよい情報を最初からAIへ送らない設計です。

## 困ったとき

- DMが届かない: 相手がまだサーバーにいるか、サーバーメンバーからのDMを許可しているか確認します。
- 呼び名が見つからない: `@予定管理 呼び名一覧を見せて`で確認します。
- 会議が複数あって回答できない: `MEET0001 参加`のように会議IDを付けます。
- 会議URLを直したい: `@予定管理 URL`と送ります。複数候補なら会議カードへBotをメンションして返信します。
- Botが日時を理解できない: Botが表示した「足りない項目」を追加して送り直すか、`/meeting create`を使います。
- 自分の通知が分からない: `/meeting my-reminders`を使います。
